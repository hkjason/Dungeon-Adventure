using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Exit : MonoBehaviour
{
    /*
    public void OnTriggerEnter2D(Collider2D collision)
    {
        if(collision.tag=="Player")
        {
            switch(this.gameObject.name)
            {
                case "Right":
                    StartCoroutine(DungeonGenerator.instance.Fade(0));
                    DungeonGenerator.instance.fromLeft=true;
                    break;
                case "Top":
                    StartCoroutine(DungeonGenerator.instance.Fade(1));
                    DungeonGenerator.instance.fromBot=true;
                    break;
                case "Bottom":
                    StartCoroutine(DungeonGenerator.instance.Fade(2));
                    DungeonGenerator.instance.fromTop=true;
                    break;
                case "Left":
                    StartCoroutine(DungeonGenerator.instance.Fade(3));
                    DungeonGenerator.instance.fromRight=true;
                    break;
            }
        }
    }
    */
}